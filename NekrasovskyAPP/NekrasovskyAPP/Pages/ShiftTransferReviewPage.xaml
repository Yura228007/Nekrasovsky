<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NekrasovskyAPP.Pages.ShiftTransferReviewPage"
             BackgroundColor="{StaticResource BackgroundDark}"
             Title="Проверка остатков">

    <Grid RowDefinitions="Auto,*,Auto,Auto" Padding="16" RowSpacing="12">

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Spacing="4">
            <Label Text="Проверка остатков"
                   FontSize="20"
                   FontAttributes="Bold"
                   TextColor="{StaticResource White}" />
            <Label Text="Проверьте остатки перед подтверждением передачи смены"
                   FontSize="13"
                   TextColor="{StaticResource Gray400}" />
        </VerticalStackLayout>

        <!-- Stock Items List (без индивидуальных галочек) -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding StockItems}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border BackgroundColor="{StaticResource BackgroundCard}"
                            StrokeThickness="0"
                            Padding="14"
                            Margin="0,4">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0"
                                    BackgroundColor="{StaticResource CardMaterials}"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    StrokeThickness="0"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Label Text="📦" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                <Label Text="{Binding Name}"
                                       FontSize="15"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource White}" />
                                <CollectionView ItemsSource="{Binding Warehouses}" HeightRequest="60">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Label FontSize="12" TextColor="{StaticResource Gray400}">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="{Binding WarehouseName}" />
                                                        <Span Text=": " />
                                                        <Span Text="{Binding Quantity}" TextColor="{StaticResource Primary}" />
                                                        <Span Text=" " />
                                                        <Span Text="{Binding MeasuringType}" />
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </VerticalStackLayout>
                            <Border Grid.Column="2"
                                    BackgroundColor="{StaticResource BackgroundElevated}"
                                    StrokeThickness="0"
                                    Padding="10,6"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>
                                <Label Text="{Binding TotalQuantity, StringFormat='Итого: {0}'}"
                                       FontSize="13"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource White}" />
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Одна общая галочка подтверждения -->
        <Border Grid.Row="2"
                BackgroundColor="{StaticResource BackgroundCard}"
                StrokeThickness="0"
                Padding="14">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="12"/>
            </Border.StrokeShape>
            <HorizontalStackLayout Spacing="12">
                <CheckBox x:Name="ConfirmCheckbox"
                          CheckedChanged="OnConfirmCheckChanged"
                          Color="{StaticResource Primary}" />
                <Label Text="Я проверил(а) все остатки и подтверждаю их корректность"
                       FontSize="14"
                       TextColor="{StaticResource White}"
                       VerticalOptions="Center" />
            </HorizontalStackLayout>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="12">
            <Button Grid.Column="0"
                    Text="Отмена"
                    Clicked="OnCancelClicked"
                    BackgroundColor="{StaticResource BackgroundCard}"
                    TextColor="{StaticResource White}"
                    CornerRadius="12"
                    HeightRequest="48" />
            <Button Grid.Column="1"
                    x:Name="ConfirmButton"
                    Text="Подтвердить"
                    IsEnabled="False"
                    Clicked="OnConfirmClicked"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource White}"
                    CornerRadius="12"
                    HeightRequest="48" />
        </Grid>
    </Grid>
</ContentPage>
